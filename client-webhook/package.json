{
  "name": "client-webhook",
  "version": "1.0.0",
  "description": "Fake server to simulate the client server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.1"
  }
}
